#include "main.h"
/**
 *add - runtime begins here
 *@a: one parameter for operations
 *@b: other parameter for operation
 *
 *Description: function that adds two integers and returns the result
 *Return: returns sum of two integers
 */
int add(int a, int b)
{
	return (a + b);
}
